import { ElipsisText } from './elipsis-text';

describe('ElipsisText', () => {
  it('should create an instance', () => {
    const directive = new ElipsisText();
    expect(directive).toBeTruthy();
  });
});
